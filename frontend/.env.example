# Set this to your Render backend URL in Vercel project settings.
# The WebSocket URL is derived automatically (http→ws, https→wss).
# Leave empty to use same-origin proxy (local dev via vite proxy).
VITE_API_URL=https://revenue-autopilot-backend.onrender.com
